function coutingSort(arr) {
  let range = Math.max(...arr);

  let counts = new Array(range);
  count.fill(0);
  let output = new Array(arr.length);
  output.fill(0);

  arr.forEach(function(val) {});
}
